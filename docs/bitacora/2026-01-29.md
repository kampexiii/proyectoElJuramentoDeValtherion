# 2026-01-29

- Implementadas migraciones `seasons`, `season_race_rankings`, `season_race_winners`.
- Añadidos modelos: `Season`, `SeasonRaceRanking`, `SeasonRaceWinner`.
- Añadidos comandos Artisan: `season:ensure-current`, `season:close`.
- Verificado con tinker y comprobando en BD (SQLite/DBeaver): `seasons` existe (2 filas: actual y anterior), `season_race_rankings` presente pero sin filas (no hay razas pobladas), `season_race_winners` existe.
- La vista pública del landing (`guest.sections.cronica_mensual`) ahora obtiene la clasificación del mes anterior desde la BD y muestra la raza ganadora si existe.
- Configurada BD a SQLite local (database/database.sqlite) para desarrollo.
- Creado usuario admin: email `pablosevillanoa90@gmail.com`, contraseña `12345678`.
- Añadida columna `plan` a tabla `users` para compatibilidad con seeder.
- Quitada sección "Información de Cuenta" del home; ahora home tiene personaje y ranking en 2 columnas.
- Modificado fallback: si no hay clasificación del mes pasado, mostrar todas las razas ordenadas alfabéticamente con puntos 0.
- Verificado con tinker y comprobando en BD (SQLite/DBeaver) que la crónica mensual carga sin romper y usa fallback si no hay datos.
- routes/web.php queda solo para rutas
- lógica de crónica movida a MonthlyChronicleService
- landing y home consumen el mismo servicio
