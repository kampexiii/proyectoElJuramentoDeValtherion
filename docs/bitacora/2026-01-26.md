## 2026-01-26 — Consolida prólogo y ajustes menores

- Se creó `resources/views/guest/sections/prologo.blade.php` como la única sección de prólogo (teaser de 3 párrafos).
- Se reemplazaron referencias a `_prologo_short`, `_prologo_full` y `_prologo_text` para apuntar a la sección consolidada.
- La página de lectura `resources/views/guest/prologo.blade.php` ahora incluye la sección consolidada.
- Se añadió la variable `--beige` y una regla CSS en `public/css/landing-theme.css` para que el botón "Acceder" en el `#hero` tenga color beige en estado normal.

Próximo: correr `npm run build` y limpiar vistas con `php artisan view:clear` para desplegar cambios.

---

Auditoría (uso real hoy):

- Vistas usadas directamente por rutas activas: `guest.welcome` (landing), `game.home.index`, `admin.index`, y las vistas de auth/profile/misceláneas referenciadas en `routes/web.php`.
- Se detectó que el prólogo debe ser una **sección** incluida en la landing. Por tanto, la única fuente válida es ahora `resources/views/guest/sections/prologo.blade.php`.

Archivos eliminados o consolidados hoy:

- Eliminados (o consolidados y retirados):
	- `resources/views/guest/_prologo_short.blade.php` (eliminado)
	- `resources/views/guest/sections/_prologo_full.blade.php` (eliminado)
	- `resources/views/guest/sections/_prologo_text.blade.php` (eliminado)
	- `resources/views/guest/prologo.blade.php` (página dedicada; retirada: el prólogo es sección en landing)

Archivos modificados (motivo):

- `routes/web.php` — se eliminó la ruta `/prologo` para evitar mantener una página separada.
- `resources/views/guest/welcome.blade.php` — ahora incluye `guest.sections.prologo` directamente.
- `resources/views/guest/sections/prologo.blade.php` — nueva única sección del prólogo (teaser + CTA).
- `public/css/landing-theme.css` — añadidos estilos de pergamino y regla para que `#hero .btn-outline-light` muestre texto beige en estado normal.
- Varios layouts/views menores actualizados por coherencia visual (listados en commits).

Tareas pendientes (23-25) revisadas y completadas:

- Viernes 23: Ajuste de badge premium y evitar solape con títulos — implementado (CSS en `landing-theme.css`, y `._razas_grid` integrado).
- Sábado 24: Reestructuración de las partials del prólogo — consolidado a una única sección y eliminadas viejas partials.
- Domingo 25: Revisión de includes y limpieza de placeholders — eliminados includes de una sola línea y placeholders obsoletos.

Rutas y cambios de infraestructura:

- `routes/web.php` — se removió la ruta dedicada `/prologo`. El prólogo queda accesible desde la landing (`/`).

Comprobaciones realizadas:

- Home carga incluyendo `guest.sections.prologo` — verificado por inclusión en `guest/welcome.blade.php`.
- No quedan partials de prólogo en `resources/views/guest/sections/` aparte de `prologo.blade.php`.
- El botón `Acceder` en `#hero` tiene regla en `public/css/landing-theme.css` para color beige en estado normal.

Pasos de verificación sugeridos:

1. `npm run build`
2. `php artisan view:clear`
3. Abrir `/` y revisar que el prólogo aparece y que no aparecen errores en consola o logs.

---

Resultado: limpieza completa del conjunto de prólogos duplicados, consolidación en una única sección y ajuste visual del hero. Los cambios se registran en commits separados (ver historial reciente). 

---

## 2026-01-26 — Normaliza avatares de razas y elimina borde interior con CSS

- Convertidos 13 avatares PNG (8-9MB cada uno) a WebP 256x256 (11-17KB) con nombres normalizados (slugs).
- Actualizada ruta de imágenes en `_razas_grid.blade.php` a `/assets/common/razas/{slug}.webp`.
- Implementado CSS con `transform: scale(1.22)` y `object-position: 50% 40%` para recortar borde circular interior.
- Añadidas clases opcionales `.avatar-zoom-tight`, `.avatar-zoom-wide`, `.avatar-pos-up`, `.avatar-pos-center` para ajustes específicos.
- Archivos tocados: `resources/views/guest/sections/_razas_grid.blade.php`, `resources/css/landing/theme.css`.
- Assets generados: 13 archivos WebP en `public/assets/common/razas/` (total ~180KB vs ~100MB originales).

---

## 2026-01-26 — Reestructura layout interno de cards de razas (footer reservado) y evita que avatar pise texto

- Reorganizado layout interno de cards: `.race-content` (título + descripción) arriba con `flex:1 1 auto`, `.race-footer` abajo con `margin-top:auto` conteniendo avatar + estado/premium.
- Eliminado posicionamiento absolute del avatar para evitar solapes; ahora vive dentro del footer.
- Integrado badge "Premium" como pill dentro del footer (derecha) en lugar de absolute corner.
- Añadido `line-clamp-2` al título para máximo 2 líneas.
- Archivos tocados: `resources/views/guest/sections/_razas_grid.blade.php`, `resources/css/landing/theme.css`.
- Nuevas clases CSS: `.race-content`, `.race-footer`, `.race-status`, `.race-premium-pill`.

---

## 2026-01-26 — Reestructura layout interno de cards de razas a 4 zonas (header/main/aside/footer) y elimina solapes

- Implementado CSS grid con 4 áreas: aside (avatar), header (título), main (descripción), footer (estado/premium).
- Avatar en columna fija de 72px, nunca solapa texto.
- Eliminado posicionamiento absoluto; layout robusto con grid-template-areas.
- Título y descripción con line-clamp a 2 líneas.
- Footer separado abajo con space-between para estado/pill.
- Archivos tocados: `resources/views/guest/sections/_razas_grid.blade.php`, `resources/css/landing/theme.css`.
- Nuevas clases: `.race-card-body`, `.race-aside`, `.race-header`, `.race-main`, `.race-footer`, `.race-status`, `.race-pill-premium`.

---

## 2026-01-26 — Cambia layout de cards de razas a horizontal (avatar izquierda, texto derecha)

- Reorganizado grid a horizontal: aside (avatar) izquierda, content (texto) derecha.
- Content con flex column: header (título), main (descripción flex:1), footer (estado/premium).
- Avatar centrado verticalmente en aside, sin solapar texto.
- Mantiene altura consistente y legibilidad como bestiario antiguo.
- Archivos tocados: `resources/views/guest/sections/_razas_grid.blade.php`, `resources/css/landing/theme.css`.

---

## 2026-01-26 — Alinea footer de cards a la derecha y reduce tamaño de texto

- Footer (Disponible/Premium) alineado a la derecha en el bloque de texto con justify-content:flex-end.
- Tamaño de fuente reducido: status .8rem, premium pill .75rem para mejor proporción.
- Mantiene legibilidad y estilo bestiario.

