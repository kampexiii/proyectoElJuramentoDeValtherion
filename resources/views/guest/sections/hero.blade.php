<section class="mb-5 text-center p-5 bg-black border border-secondary rounded">
    <h1 class="display-4 text-warning fw-bold">El Juramento de Valtherion</h1>
    <p class="lead text-light mb-4">Esta sección será el Hero: gancho + botones</p>
    <div class="d-flex justify-content-center gap-3">
        <a href="{{ route('register') }}" class="btn btn-primary btn-lg">Registrarse</a>
        <a href="{{ route('login') }}" class="btn btn-outline-light btn-lg">Login</a>
    </div>
</section>
